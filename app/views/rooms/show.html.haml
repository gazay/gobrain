#center
  #messages
    %ol
      %li= "#{@user.name} logged in"
  %form{ :method => 'post', :action => '', :id => 'new' }
    %textarea
    %input{ :type => 'submit', :value => 'Send ↑' }
    %p
      New line: Ctrl + Enter
    %p
      Italic: _<em>italic</em>_
    %p
      Bold: *<strong>bold</strong>*
#panel
  #about
  %ul#users
    - User.inside(@room).each do |user|
      - if @user.id == user.id
        %li{ :id => "user-#{user.id}", :class => "me" }
          = user.name
      - else
        %li{ :id => "user-#{user.id}" }
          = user.name
